#!/bin/bash

# Подключаем общую библиотеку функций
source /usr/local/bin/player-functions

case "$1" in

    # Стартуем с новой флешки:
    start)
        start_playback
        ;;

    # Флешку вынули:
    stop)
        stop_playback
        ;;
esac
